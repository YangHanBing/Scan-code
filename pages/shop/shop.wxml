<view class="container">
  <view class="swiper-wrap">
    <i-swiper data="{{bannerData}}"></i-swiper>
  </view>
  <view class="main">
    <i-code-button count="{{goodsList.length}}" status="{{status}}" bind:getResult="handleSaoCode"></i-code-button>
  </view>
</view>
